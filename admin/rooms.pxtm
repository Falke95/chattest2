<!DOCTYPE html>
<html lang="en">

<head>
    <title>ACP: Groups</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, height=device-height, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <link rel="stylesheet" href="admin/<?php echo isset($settings['acp_css']) ? $settings['acp_css'] : ''; ?>">
    <script src="admin/admin.js"></script>
</head>

<body class="x_global x_overal">
    <div class="holder">
        <h2><?php echo ($action == 'add') ? 'Create Group' : 'Edit Group'; ?></h2>
        <hr>
        <!-- Rest des Formulars hier ... -->
        <hr>
        <h2>Groups</h2>
        <table>
            <tr>
                <th>ID</th>
                <th>Name</th>
                <th>Action</th>
            </tr>
            <?php if (isset($groups) && is_array($groups)) : ?>
                <?php foreach ($groups as $group) : ?>
                    <tr>
                        <td><?php echo $group['id']; ?></td>
                        <td><?php echo $group['name']; ?></td>
                        <td>
                            <a href="admin.php?q=groups&editgroup=1&group_id=<?php echo $group['id']; ?>">Edit</a> |
                            <a href="admin.php?q=groups&deletegroup=1&group_id=<?php echo $group['id']; ?>">Delete</a>
                        </td>
                    </tr>
                <?php endforeach; ?>
            <?php endif; ?>
        </table>

        <hr>

        <!-- Hier ist die "Gruppen erstellen" Kategorie und das Menü -->
        <div id="createGroupCategory" class="pointer">Create Group</div>
        <form id="createGroupForm" style="display: none;" method="post" action="admin.php?q=groups">
            <!-- Formular für das Erstellen und Bearbeiten von Gruppen -->
        </form>
    </div>

    <!-- Hier ist das Menü -->
    <div>
        <?php
        $res = neutral_query('SELECT * FROM ' . $dbss['prfx'] . '_rooms ORDER BY zorder,id ASC');
        $rooms = '';
        $rcodecn = 0;

        while ($row = neutral_fetch_array($res)) {
            // Code für die Raumliste hier ...
        }
        ?>
        <!-- Der Rest des Codes für die Raumliste ... -->
    </div>

</body>

</html>
